// Migration note:
// The app previously used easy_localization with an AssetLoader.
// The project now uses Flutter gen-l10n (ARB) and AppLocalizations.
// This file remains only to avoid breaking older imports.

class Translation {
  const Translation._();
}
